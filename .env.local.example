# Copy this to .env.local for local testing, or set it as an env var on Railway.
# Leave unset locally and anyone can use the app without logging in.
# Once set, visitors must enter this password once (stored in a cookie) before
# they can reach the generator or its API.
APP_PASSWORD=
